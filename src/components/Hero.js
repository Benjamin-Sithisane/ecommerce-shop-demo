import FeaturedItem from "./FeaturedItem";

const Hero = () => {
  return (
    <section className='h-[800px] flex bg-slate-200 justify-center'>
      <FeaturedItem />
    </section>
  )
};

export default Hero;
